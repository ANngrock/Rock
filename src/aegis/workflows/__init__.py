"""Воркфлоу хода (F8): повторяемость сайд-эффектов, компенсации и версия кода процесса.

Модуль намеренно отделён от ``agents``: это не «ещё один слой вызова модели», а контракт
исполнения — тот же, что раньше держали напоминания (таблица + идемпотентный тик, ADR-0010).
Temporal здесь — реализация, а не смысл: отсутствие кластера обязан переживать тот же код
(:mod:`aegis.workflows.turn` через локальный раннер), иначе наблюдаемость и работа бота оказались
бы связаны (ADR-0021).
"""
