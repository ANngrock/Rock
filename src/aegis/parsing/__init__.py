"""Парсер-наблюдатель: универсальное чтение страниц, лент и публичных Telegram-каналов.

Философия повторяет напоминания и watches: модель переводит слова в аргументы, а фактчитает
детерминированный код. Три семейства источников, одно горло, одна дедупликация:

* ``web`` — произвольная страница: trafilatura, fallback-текст, метаданные, ссылки;
* ``rss/atom/jsonfeed`` — ленты (и страницы, которые оказываются лентой по content-type);
* ``tg_channel`` — публичные каналы через t.me/s (web-превью Telegram): без API-ключей,
  с пагинацией курсором, медиа-ссылками и автором-форварда.

Приватные каналы — НЕ сюда: им нужен юзербот-мост (cognition.inbox), и честный ответ на
«подпишись на мой приватный канал» — «для этого есть USERBOT, а не парсер».

Тела предметов шифруются (vault.seal_text) когда в процессе есть ключарка; заголовок и выдержка
остаются открытыми — по ним строятся превью и поиск, а источники по определению публичные.
"""

from __future__ import annotations

__all__ = ["FeedsReport", "ScanOutcome", "run_feeds"]

from aegis.parsing.watcher import FeedsReport, ScanOutcome, run_feeds
