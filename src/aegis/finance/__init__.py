"""Finance (шаг 3): ledger двойной записи, счета, валюты, бюджеты, прогнозы.

Пока только граница контекста: домены не импортируют друг друга (проверяется import-linter),
поэтому реализация подключится здесь, а инструменты станут тонкими обёртками над use cases.
"""
