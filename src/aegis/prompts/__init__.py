"""Каталог промптов (Markdown + frontmatter) как часть пакета.

Сами тексты — не код: их правят часто, и правка должна быть читаемой в диффе. Каталог попал внутрь
пакета специально: `COPY src ./src` в образе и `package-data` в сборке тогда гарантируют, что
промпт в контейнере — ровно тот же файл, что и в репозитории, а не «дефолт из кода, когда файла не
нашли».

Читает их :mod:`aegis.platform.prompts`; версия и хэш каждого попадают в decision record
(:mod:`aegis.governance.recorder`).
"""
